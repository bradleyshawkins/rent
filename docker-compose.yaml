version: '3.6'
services:
  mysql:
    image: postgres:13.2
    restart: always
    networks:
      - network
    ports:
    - 5432:5432
    environment:
      POSTGRES_PASSWORD: 'password'
      POSTGRES_DB: 'rent'
networks:
  network:
    driver: bridge